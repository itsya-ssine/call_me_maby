"""Call me maybe — Function calling tool using constrained decoding.

Modules:
    __main__:
        CLI entry point.
    models:
        Pydantic data models.
    file_io:
        JSON file loading / saving utilities.
    constrained_decoder:
        Token-by-token JSON-schema-aware constrained generation.
    function_selector:
        Prompt building and LLM-driven function selection.
"""
